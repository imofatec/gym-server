import { scheduleDependencies } from './schedule-dependencies.ts'
import { userDependencies } from './user-dependencies.ts'

export const user = {
  dependencies: userDependencies,
}

export const schedule = {
  scheduleDependencies
}
